services:
  dashboard:
    build: .
    ports:
      - "8787:8787"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      # Persist SQLite auth DB across container restarts
      - foxops-data:/data

volumes:
  foxops-data:
